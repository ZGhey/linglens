import { describe, it, expect } from 'vitest'

// Placeholder test proving the test runner is wired up (ticket 01).
describe('test runner', () => {
  it('runs', () => {
    expect(true).toBe(true)
  })
})
